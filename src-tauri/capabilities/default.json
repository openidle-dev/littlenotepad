{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LittleNotepad default capabilities — IPC restricted to tauri://localhost only",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
